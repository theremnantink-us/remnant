{
  "name": "remnant",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node --watch server/index.js",
    "postbuild": "cp -r js dist/js && cp -r css dist/css && cp -r fonts dist/fonts && cp sw.js dist/sw.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "vite": "^8.0.3"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}